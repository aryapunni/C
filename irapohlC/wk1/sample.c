#include<stdio.h>

int main(void)
{
    int a, b, c, sum;
    scanf("%d%d%d", &a, &b, &c);
    printf("a = %d, b = %d, c = %d", a, b, c);
    sum = a + b + c;
    printf("sum = %d", sum);
    return 0;

}
